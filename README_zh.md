**服务器**：

**datatsets/**（**SELECTED_DIRECTORY** 即数据集在服务器端的绝对路径）、本项目（[nerf-3dgs-benchmark](https://github.com/mekeny1/nerf-3dgs-benchmark)）放于用户根目录

**nerf-3dgs-benchmark/**：

各个子仓库目录下 `clone --recursive` 对应fork版的仓库

**容器**：

数据集路径为：**/workspace/datasets**

各个子仓库在 **nerf-3dgs-benchmark** 对应路径下用vsc单独打开，提示构建容器时开始构建容器/镜像

